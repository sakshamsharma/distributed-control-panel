{
    "servers": [
        {
            "name": "localhost",
            "ip": "127.0.0.1"
        }
    ],
    "binary": "yo",
    "arg-gen-lambda": "lambda peers_with_addr: lambda self: ('single ') + ' '.join(['--seed ' + peer.addr for peer in peers_with_addr]) + (' -p ' + str(self.port))"
}
